# Contributing

感谢你为 TrustGuard Docs Skills 提交改进。

## 提交前检查

- 保持每个 skill 的目录名、`SKILL.md` frontmatter 中的 `name` 和调用名一致。
- 不要在 skill 指令中虚构数据、来源、性能指标或项目资质。
- 改写文档时保留用户明确要求保留的事实、技术术语、数字和格式约束。
- 修改架构图 skill 时，保持源图节点、标签、箭头方向和拓扑关系不变。
- 不要提交密钥、个人隐私、生成的临时文件或本地编辑器配置。
- 对所有受影响的 skill 运行 `skill-creator` 的 `quick_validate.py`。

## Pull request 建议

请在 PR 描述中说明：

1. 修改了哪个 skill 和哪些文件；
2. 解决了什么具体问题，或新增了什么可复用能力；
3. 验证命令及其结果；
4. 如果涉及参考图、第三方文本或图标，说明来源和许可证。

小而聚焦的 PR 更容易审阅。涉及行为变化时，请同时更新对应 README 或参考文档。
